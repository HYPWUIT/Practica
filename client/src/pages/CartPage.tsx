import PagePlaceholder from '../components/PagePlaceholder'

function CartPage() {
  return (
    <PagePlaceholder
      title="Your cart"
      phase="Phase 6"
      description="Line items with quantity steppers, remove actions, a running subtotal, and an empty state."
    />
  )
}

export default CartPage
