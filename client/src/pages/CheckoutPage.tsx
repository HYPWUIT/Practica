import PagePlaceholder from '../components/PagePlaceholder'

function CheckoutPage() {
  return (
    <PagePlaceholder
      title="Checkout"
      phase="Phase 6"
      description="Three steps — shipping, payment, review — on one react-hook-form instance validated per step."
    />
  )
}

export default CheckoutPage
